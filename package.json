{
  "name": "matutum-plotnav",
  "version": "1.0.0",
  "description": "Capacitor wrapper for Matutum PlotNav",
  "scripts": {
    "start": "node server.js",
    "install-capacitor": "npm install",
    "add-android": "npx cap add android",
    "build-apk": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
